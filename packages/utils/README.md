# @nim-ui/utils

Shared utility helpers used across the Nim UI monorepo.

Current exports:

- `cn()` for `clsx` + `tailwind-merge`
- `canUseDOM()` for SSR-safe DOM checks
- `dataState()` for tiny state attribute helpers
